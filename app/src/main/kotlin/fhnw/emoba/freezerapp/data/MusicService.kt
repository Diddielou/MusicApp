package fhnw.emoba.freezerapp.data

interface MusicService {
    // TODO fetch data from Deezer

    // TODO what functions are needed here?

    /*
    fun requestMusic(searchTerm: String): List<Artist>

    fun requestMusic(searchTerm: String): List<Album>

    fun requestMusic(searchTerm: String): List<Track>

    fun requestMusic(searchTerm: String): List<Radio>
     */

}